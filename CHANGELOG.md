# 更新历史

拼豆工具（fuse-beads-tool）的版本记录。最新功能说明见 [README.md](README.md)。

## 0.8.0-beta（公测准备）

- **版本与门面**：版本号统一为 0.8.0-beta；README 顶部增加版本徽章。
- **图标统一**：浏览器 / EXE / 系统托盘三处图标统一为 favicon 设计（蓝底 2×2 拼豆格），打包脚本同步复刻，避免三处漂移。
- **文档**：README 与 CHANGELOG 拆分；功能条目精简；界面与文档版本号去掉 `v` 前缀；许可链接修正；新增「已知限制」；顶栏提示建议定期备份 `data` 目录。
- **口径**：导出文件包含用于归属校验的标识（不影响图案内容）。
- **代码质量**：开启 noImplicitAny 与 strictNullChecks；npm 后端脚本支持 `PYTHON` 环境变量；接入前端 c8 与后端 coverage.py 覆盖率（仅报告不设门槛）；清理 Docker 打包残留。

- **0.7.0（发布准备）**：面向发布的身份 / 署名 / 版权信息完善。
  - 界面：顶栏「拼豆工具」右侧新增版本号小字（0.7.0，随发布自动同步）。
  - 导出：图案与图例之间新增右侧署名小字「由 解音知弦 (SoulString) 研发的拼豆工具生成」，导出预览与产物一致，PDF 每页均显示；署名行高计入图片与 PDF 自适应缩放。
  - 版权：导出图片 / PDF 自动写入版权元数据（JPG EXIF / PNG tEXt / PDF 文档信息），并叠加隐写水印用于标注生成来源。
  - 帮助：常见问题说明与帮助新增「联系作者」（GitHub Issues + 邮箱 + 反馈模板）与「关于」（版本 / 作者 / 许可 / 第三方致谢 / 隐私 / 数据位置）。
  - 打包：EXE 携带版本 / 产品名 / 版权等文件属性与 LICENSE；新增 GitHub Releases 自动发布流水线（tag 触发构建 EXE + SHA256）。
  - LICENSE 版权行统一为 `SoulString-Dev (解音知弦)`。
- **0.6.0（正式版）**：新增「镜像」编辑工具与界面 / 交互优化。
  - 镜像工具：左侧编辑工具新增「镜像」（G 键 / 按钮），编辑工具改为两行布局；勾选「水平」/「垂直」即时预览，原图与拼豆图同步翻转，点击「应用」才实际生效并记入撤销 / 重做，ESC 放弃；已应用的镜像重新进入时勾选状态保持；快照切换时对比原图显示方向随快照同步。
  - 移除旧版「导入 / 重新压缩时水平镜像」勾选（改为编辑工具内手动应用）。
  - 工具栏布局优化：帮助下拉与操作提示移入标题行（夜间模式左侧）、颜色数量移到重新压缩右侧并加分隔线，整体减少一行空间。
  - 颜色数量滑块：需要确认时立即弹出确认框（不再等防抖）；导入 / 重新压缩 / 修改颜色数量后自动回到选择模式。
  - 文案统一：「使用问题修复」改为「常见问题说明与帮助」。
  - 累积更新（0.5.5 – 0.5.8）：一致性 / 健壮性 / 性能 / 可维护性重构并接入 ruff / pyright；P0 清理（统一 popup.js、els / DOM 契约检查）；默认色板更换为 mard-221-github；快捷键说明文档与多项修复。
- **0.5.4（维护版）**：修复高亮遮罩与魔棒快捷键。
  - 颜色清单高亮改为仅亮度自适应描边，去掉像素格上方的半透明黑 / 白遮罩；Playwright 用例改为断言「格内不再叠加遮罩、描边仍存在」。
  - 魔棒快捷键由 `M` 改为 `T`，同步更新 README、`static/docs/shortcuts.md` 与魔棒按钮提示。
- **0.5.3（维护版）**：健壮性 / 一致性修复、性能优化与测试工具链完善。
  - 健壮性：修复 D 键九宫格悬停预览未真正渲染候选色（补像素级回归测试）；修复 lint 格式问题；颜色数量滑块输入防抖 + 防重入，避免多个确认弹窗叠加；自动保存 PUT 串行化，慢写入期间排队补写最新状态。
  - 一致性：明确像素编辑同样计入「打开项目将覆盖」确认（README 与实现对齐）；目标像素量预设、视图缩放 / 平移、侧边栏折叠等所有设置与视图变更统一调度自动保存。
  - 性能：目标像素量上限 80000 → 30000（前后端同步）；画布面积上限收紧至 3000 万像素²，防止极端长宽比内存失控；快照上限按网格规模自适应（历史区约 4MB 体积预算）；压缩结果严格不超过目标像素量；画笔 / 橡皮笔划中只增量重绘脏格，笔划结束统一全量刷新。
  - 测试与工具链：重写并接线 Token 认证端到端测试（auth_test），Playwright 纳入 devDependencies，UI / 渲染一致性 / 认证三套 Playwright 测试全部进入 CI；补齐快照体积预算与笔划增量重绘回归测试；新增核心领域类型声明（types/domain.d.ts）作为后续开启 strict 的类型地基。
- **0.5.2（维护版）**：界面细节统一与文案整理。
  - 下拉框：所有 select 内边距统一为 `5px 28px 5px 8px`；颜色距离加宽至完整显示「Lab（人眼感知）」；工作区「空位显示」高度统一为 31px；导出弹窗「空位显示」宽度与格式下拉一致；「常见问题说明与帮助」按钮改用与目标像素量一致的 SVG 箭头。
  - 导出：导出图片默认勾选「显示行列号」。
  - 文案：统一按钮、标签、提示与确认弹窗的措辞与标点。
- **0.5.1（维护版）**：面向长期维护的代码质量重构与测试补齐。
  - 一致性：文件名清洗 / 原图落盘 / 导出校验 / PDF 页面构建收敛为单一实现；前后端颜色数学、paletteHash、hex 归一化、版本号增加同步测试。
  - 健壮性与安全：损坏 `.ssfbp`（解压炸弹 / 重复段名 / 未知标志）返回 JSON 400；导出非法输入不再 500；登录失败限流；`APP_SECRET` 不再从 Token 派生；Windows 保留名自动加前缀；无效上传不落盘。
  - 性能：PIL 单元格 numpy 批量填充；PDF 分页单趟提取与预览缓存；自动保存网格 base64 编码 + 快照数量封顶；配置列表缓存；导出预览 / 裁剪放大镜复用缓存。
  - 可维护性：app.py 运行时配置收敛到 `app.config`；版本号单一来源；schemaVersion 读取校验；快捷键注册表、目标像素量独立模块、Pointer Events 统一；状态边界与测试钩子契约化；统一日志（轮转文件）；新增 GitHub Actions CI。
  - 测试：补齐后端边界（重命名冲突 / 保留名 / 404 / 缺文件）、PDF 预览异步流程、损坏 history 恢复、应用工厂隔离、Token 门禁等用例；`npm run test:all` 串联前端、后端与 Playwright。
- **0.5.0（正式版）**：界面统一重构与无障碍收尾。
  - 弹窗体系：自绘确认 / 输入弹窗（popup.js），支持 Enter 确认、空值校验、Escape / 遮罩取消、焦点圈定与背景滚动锁定；文档、导出、登录、色板配置弹窗补齐 `role="dialog"` / `aria-modal` / `aria-labelledby`。
  - 菜单 / 组合框：使用问题修复菜单支持方向键导航与焦点还原；目标像素量改为 combobox（`role="combobox"` + aria 属性，↑↓ 选择预设、Enter 应用、Esc 关闭）。
  - 面板与按钮：左侧栏改为「编辑工具」可折叠标题头，三个面板头改真 button 并同步 `aria-expanded` / `aria-controls`；工具按钮与主题按钮补 `aria-pressed`。
  - 异步反馈：新增 `withPending`，登录、导入、重新压缩、配置增删改、项目保存 / 打开、导出均防重复提交并标记 `aria-busy`；toast 增加 success / error 视觉分级。
  - 视觉令牌：收敛 radius / surface / focus / input / border / btn 等设计令牌，统一全部 select 自绘箭头外观与全局 focus-visible；修复九宫格夜间模式白底与下拉箭头夜间悬停平铺。
  - 色板配置：移入工具栏「色板配置」弹窗（颜色距离与重新压缩之间），颜色表带固定表头、16 进制输入完整显示；MARD 221 色表内嵌代码，全新 data 目录自动创建并作为默认配置。
  - 编辑工具：颜色列表改为 6 列紧凑色块（色号 + 数量在色块内、顶对齐），未导入图片时也可提前选择画笔。
  - 术语与行为：事务 → 快照、事务历史 → 快照清单；「清空所有快照」只删除快照与撤销记录，保留当前画布与图片；删除正在使用的颜色不再弹确认；重新压缩合并为单个确认弹窗。
  - 其它修复：适应窗口定位（扣除面板内边距）、导出色号字号缩小、目标像素量控件缩短、版权署名改为 SoulString-Dev。
  - 测试：新增组件契约测试 `tests/dom_contract_test.mjs`（ARIA / 焦点 / pending / 设计令牌），`npm test` 改为完整 Node 套件。
- **0.4.1（补丁）**：新增 [docs/ui-contract-audit.md](docs/ui-contract-audit.md) UI 控件契约审计表，梳理按钮 / 菜单 / 弹窗 / 表单 / 异步反馈 / 无障碍的现状与目标契约，作为后续界面统一重构的基线。
- **0.4.0（正式版）**：保存 / 打开 `.ssfbp` 统一走浏览器（移除后端 PowerShell / tkinter / Win32 保存框与项目路径跟踪）；修复 Ctrl 拖拽反选逐格响应、D 键九宫格改色未标记保存、快捷键说明（Ctrl+Shift+Z 重做）、目标像素量下限与 400 / 2000 / 4000 / 8000 预设、导出弹窗 Escape 重置并取消焦点、颜色清单高亮闪烁、保存文件名取原图名、PDF A4 分页默认竖版等；重构：统一网格修改入口、清理重复代码、状态管理收敛、模块划分与文件拆分、一致性与风格统一、渲染与交互性能优化、无障碍 / 触控完善；工具链引入 Biome + TypeScript checkJs 并补齐测试脚本，DOM 行为测试拆分为 editor / state / ui 分组并共享 harness。
- **0.3.1（测试版）**：新增魔棒工具（M 键 / 按钮），按「容差」选中四向连通的相似色；容差 0-100 可调并持久化，Shift 点击追加选区；编辑工具行新增第五个按钮。自动保存扩展为记录视口、当前工具 / 画笔色、选区、dirty、撤销 / 重做栈与后端原图引用；原图按内容哈希落盘并支持直接重新压缩；项目与事务快照增加色板哈希，配置缺失 / 不一致时自动创建恢复配置；新增 `.ssfbp` 项目文件保存 / 打开（保存 / 打开统一走浏览器）；新增 PDF 导出（A4 单页 / A4 多页 / A3 或 A4），多页模式带总览页、跨页行列号与分页图例。
- **0.3.0（正式版）**：由 0.2.2 测试版转正，完成大规模前端重构与多项修复：
  - 前端重构：main.js 按领域拆分模块、状态单一来源、渲染入口收敛、动态列表事件委托、命名清理、常量收敛与 JS/CSS/Python 跨语言同步测试、测试钩子契约化；功能行为不变。
  - 裁剪工具（R 键 / 按钮）：红色边框 + 40% 蒙版、选中 / 拖拽边（含行列号条内）、点击平行格线移动、自动裁剪、应用生成「裁剪前」快照并支持整体撤销 / 重做；低缩放时显示 11×11 放大镜（含行列号条与外部区域，夜间配色）。
  - 日间 / 夜间模式（跟随系统并记忆偏好）、使用问题修复下拉菜单；快捷键扩展 Q/W/E/R 与 Delete 清除选中格。
  - 撤销栈支持结构型步骤（尺寸变化操作可整体撤销 / 重做）；「目标像素量」可输入 + 预设下拉（400 / 2000 / 4000 / 8000，上限 80000、默认 4000）；缩放细节分层隐藏。
  - 修复：裁剪拖拽边界失效、裁剪蒙版四角重复变暗、夜间模式事务高亮配色、目标像素量默认值与下拉交互、导出预览图例白底、模式行高度抖动等。
- **0.2.1**：移除图案外侧透明边距，四周常驻浅蓝底行列号条（外圈无线）；网格线规范为「边缘粗黑 / 格内细灰 / 每 5 格粗灰虚线 / 每 10 格粗灰实线」；导出支持行列号与进度条；画笔未选色自动取最暗色；完成大规模代码重构（分层渲染、交互拆分、双端渲染一致性测试），功能行为不变。
- **0.2.0**：拖拽模式重构为选择模式（左键单击 / 拖拽选择、Shift 多选、ESC 清除，虚线选区按四方向连通合并）；右键改为工作区内任意位置拖拽平移；「同色选区」与「选中高亮」；有选区时单击颜色填充选区；取色按是否有选区决定返回选择或画笔；D 键九宫格悬停预览改色（单选格优先，否则悬停格）。
- **0.1.4**：hover 边框按模式区分（选择黑白虚线、画笔 / 取色 3D 凸起、橡皮边框 + X），线宽随缩放等比变化且过小自动隐藏；色号高亮按四方向连通合并；画笔 / 橡皮尺寸拖动条（1-10，边长 2n-1）。
- **0.1.3**：EXE 改为系统托盘后台常驻（单实例、双击打开网页、右键退出、可更换图标）；颜色距离延迟到「重新压缩」后生效；代码常量统一配置与后端重构。
- **0.1.2**：侧边栏折叠 / 展开（画布保持原位）、对比原图与同步拖拽、颜色清单高亮增强、原图缓存、本地直开自动打开浏览器。
